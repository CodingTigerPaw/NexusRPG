import type { FlowStrategy } from './types';

export const rollUnderStrategy: FlowStrategy = {
  id: 'ROLL_UNDER',
  label: 'Roll under target'
};

export const dicePoolStrategy: FlowStrategy = {
  id: 'DICE_POOL',
  label: 'Dice pool'
};
